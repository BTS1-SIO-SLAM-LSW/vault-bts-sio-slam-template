name: Secret scan reminder

on:
  push:
  pull_request:

jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Reminder
        run: |
          echo "Rappel pédagogique : ne commitez jamais de vrais secrets dans ce dépôt."
